using UnityEngine;

public class SpecialCardEntity : CardEntity
{
    public SpecialCardEntity(
        int id,
        string name,
        string description
    ) : base(id, name, description)
    {
    }
}